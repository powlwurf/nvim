return {
  'numToStr/Comment.nvim',
  opts = {
    -- prevent auto continuation
    padding = true,
    sticky = true, -- keep cursor position
    ignore = nil,
    -- don't insert comment on newline
    pre_hook = nil,
    post_hook = nil,
    -- add any options here
    --
    --
  },
}
